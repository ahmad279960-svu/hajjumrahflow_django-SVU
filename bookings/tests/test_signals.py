# bookings/tests/test_signals.py

# Placeholder for tests to ensure webhooks are fired correctly.
# These tests would typically use `unittest.mock.patch` to mock the
# `requests.post` call and verify that it was called with the
# correct URL and payload.